version: '3.8'
services:
  front-end:
    build:
      context: ./front-end
      dockerfile: dockerfile
    image:

    ports:
      - "9000:9000"

  back-end:
    build:
      context: ./back-end
      dockerfile: dockerfile
    image:

    ports:
      -  "5252:5252"